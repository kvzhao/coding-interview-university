
#include <string>
#include <vector>
#include <iostream>
#include <unordered_map>
#include <unordered_set>
#include <algorithm>

using std::vector;
using std::string;
using std::cout;
using std::unordered_map;
using std::unordered_set;
using std::min;
using std::abs

class Solution {
public:
    int rectangleArea(vector<vector<int>>& rectangles) {
        
    }
};


int main() {

  vector<vector<int>> rectangles = {{0,0,2,2},{1,0,2,3},{1,0,3,1}}
  const int ans = 6;
  Solution sol;

  auto ret = sol.rectangleArea(rectangles);
}